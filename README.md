
# 📊 K-NOT-Mean: N-мерный кластерный анализ на Kotlin Multiplatform

## О проекте

**K-NOT-Mean** — это интерактивное приложение для проведения и визуализации кластерного анализа (например, K-Means) N-мерных данных. Проект разработан с использованием **Kotlin Multiplatform (KMP)** и фреймворка **Compose Multiplatform** для обеспечения единого UI на различных платформах.

### 🌟 Основные возможности

* **Универсальный ввод данных:** Поддержка загрузки данных из CSV-файлов любой размерности (2D, 3D, N-D).
* **Гибкая архитектура:** Использование абстрактного класса `Point` и его наследников (`Point2D`, `NDPoint`) для эффективной многомерной векторной математики.
* **Кроссплатформенный UI:** Графический интерфейс, написанный на Compose, сфокусирован на Desktop (JVM) для локального анализа данных.
* **Интеграция CI:** Настроен пайплайн GitHub Actions для автоматического тестирования и ручной сборки релизов.

-----

## 🏗️ Структура проекта

Проект базируется на шаблоне **Kotlin Multiplatform** с упором на Desktop (JVM).

* **`/composeApp`**: Основной модуль, содержащий весь исходный код.
    * **`/composeApp/src/commonMain`**: Код, общий для всех целевых платформ (объявление `expect` функций, UI-компоненты, ядро алгоритмов кластеризации, абстрактные классы `Point`).
    * **`/composeApp/src/jvmMain`**: Код, специфичный для платформы Desktop (JVM). Здесь находится **реализация `FileDialog`** (`actual` функция) и точки входа (`main.kt`).

-----

## 🚀 Сборка и Запуск (Desktop/JVM)

Для работы с проектом вам потребуется установленный **JDK 17 или выше**.

### Запуск из IDE

Рекомендуется использовать IntelliJ IDEA. Запуск осуществляется через стандартную конфигурацию **Run Configuration** (зеленый треугольник) в панели инструментов IDE.

### Запуск из Терминала

Используйте **Gradle Wrapper** для сборки и запуска приложения.

| Платформа | Команда для запуска | Описание |
| :--- | :--- | :--- |
| **macOS / Linux** | `./gradlew :composeApp:run` | Запуск приложения в режиме разработки. |
| **Windows** | `.\gradlew.bat :composeApp:run` | Запуск приложения в режиме разработки. |
| **Сборка релиза** | `./gradlew :composeApp:packageReleaseDistributionForCurrentOS` | Создание исполняемого `.zip` или `.dmg`/`.exe` файла в папке `composeApp/build/compose/binaries/`. |

-----

## 📁 Формат входного файла (CSV)

Приложение ожидает файл в формате CSV (разделитель `,` или `;`) с обязательной строкой-заголовком.

### Правила парсинга

1.  **Координаты:** Все числовые столбцы, не являющиеся ID кластера, считаются координатами.
2.  **Кластер:** Колонка с названием `cluster`, `label`, `id` или `class` используется для предварительного окрашивания точек.
3.  **Визуализация:** Приложение всегда использует первые две найденные координаты как оси X и Y для 2D-визуализации, даже если данные N-мерные.

### Пример файла

```csv
dim1,dim2,dim3,cluster
0.1,0.5,1.2,0
0.9,0.2,0.8,1
0.5,0.4,1.0,0
```

-----

## 🧪 CI/CD и Качество кода

Проект использует **GitHub Actions** для обеспечения непрерывной интеграции и контроля качества.

* **Автоматические проверки:** При каждом коммите в ветку `main` запускается задание **Run Tests** (`./gradlew check`).
* **Ручная публикация:** Сборка и публикация релизного артефакта (`.zip`) запускается **вручную** через кнопку **Run workflow** на вкладке Actions, что гарантирует, что релиз создается только на проверенном и стабильном коде.

-----